import re

inp = input('Enter a file: ')
f = open(inp)
for line in f:
    line = line.rstrip()
    # match non blank character ([^ ]) anny number (*) of them
    t = re.findall('^From .*@([^ ]*)', line)
    if len(t) !=1: continue
    print(t)
